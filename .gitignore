# Urheberrechtlich geschuetzte Herstellerunterlagen - niemals versionieren
reference/

# Python
.venv/
__pycache__/
*.pyc

# Node (nur fuer tools/check_webui.mjs -- jsdom, nicht versioniert)
node_modules/
package-lock.json

# PlatformIO
.pio/

# KiCad
*-backups/
*.kicad_prl
fp-info-cache
*.bak
_autosave-*
*.net
/gerber/
*.ses
hardware/daughtercard/jlc/
hardware/master/jlc/
# hardware/daughtercard/manufacturing/ ist ein committetes Fertigungspaket
# (Gerber, BOM, CPL) aus tools/gen_manufacturing.py -- kein Bauartefakt.

# generierte Pruefberichte mit Zeitstempel (regenerierbar)
docs/erc-*.rpt
docs/drc-*.rpt

# private Schluessel -- nie versionieren (OTA-Signatur, siehe docs/firmware-signing.md)
*.pem
*.key
*.pkcs8
ota-signing*
ota_keys*.json

# Sonstiges
.DS_Store

# heruntergeladene Werkzeuge (FreeRouting-JAR, JRE) -- via tools/setup_freerouting.sh
tools/vendor/
